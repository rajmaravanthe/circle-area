

var radius = parseFloat(prompt("Enter radius of circle")); // get the radius from user
var area = 3.14159 * radius * radius; // area = pi*r*r (pi = 3.14159)
document.write("Area of Circle = "+ area);